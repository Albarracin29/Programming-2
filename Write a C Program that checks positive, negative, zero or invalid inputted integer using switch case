#include <stdio.h>

int main()
{
    int A;
    printf("Enter the number Here: ");
    scanf("%d", &A);
    if (A > 0)
        printf("%d is positive.", A);
    else if (A < 0)
        printf("%d is negative.", A);
    else if (A == 0)
        printf("data is invalid.", A);


    return 0;
}
